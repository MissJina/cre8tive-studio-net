'use client';
import { useState } from 'react';
// shadcn imports for UI

export default function Chatbot() {
  const [messages, setMessages] = useState([]);
  // Logic to call /api/chat, persist session, navigate (e.g., router.push('/services'))
  // System prompt in API route
  return /* Floating panel UI */;
}
